services:
  - type: web
    name: 99-nights-in-the-forest
    runtime: node
    buildCommand: npm install
    startCommand: node server/index.js
    envVars:
      - key: JWT_SECRET
        generateValue: true
      - key: NODE_ENV
        value: production
